server:
  port: 8082

spring:
  cloud:
    config:
      server:
        default-label: main
        git:
          uri: https://github.com/luisMoncadaMorales/config-data.git
          Username: ${GIT_USER}
          Password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t