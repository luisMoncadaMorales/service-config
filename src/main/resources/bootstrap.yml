server:
  port: 8082

spring:
  cloud:
    config:
      server:
        default-label: main
        git:
          uri: https://github.com/luisMoncadaMorales/config-data.git
          Username: ${GIT_USER}
          Password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t

#spring:
 # cloud:
  #  config:
   #   server:
    #    default-label: develop
     #   git:
      #    uri: file:///C:/Users/Luis.Moncada/Documents/Java/PracticaPragma/config-data
  #security:
   # user:
    #  name: root
     # password: s3cr3t